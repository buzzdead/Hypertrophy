// ExerciseDetails/LoadingIndicator.tsx
import React from "react";
import {SafeAreaView, Text} from "react-native";

const LoadingIndicator: React.FC = () => (
  <SafeAreaView>
    <Text>Loading...</Text>
  </SafeAreaView>
);

export default LoadingIndicator;
